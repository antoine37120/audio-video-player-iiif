Liste des améliorations à apporter :

- Utiliser un fichier test .iiif pour les annotations. Et donc implementer le js pour charger le contenu de ce fichier pour le convertir en json, comme le fichier annotations.json
- Modifications mineur de la config de vis.js. Il faut utiliser "start" pour fair comme ci on commençait à 0 la ligne du temps. Et "end" pour limiter à la durée du média. Et utiliser "zoomMax" à l'équivalent de 10 secondes. Et "zoomMin" à l'équivalent la durée du média.
- Au simple clic sur la timeline, continuer la lecture du média à partir de l'enmplement cliqué si la lecture est en cours. Et sinon, dire au lecteur de se positionner à l'endroit cliqué.
- Le formulaire d'ajout d'anotation doit apparaitre dans une lightbox, au centre de l'écran. Il faut ajouter un champ pour le "type" de l'item à ajouter "point", "range"; On considère que si un annotation est de type "point", il faut utiliser un champ pour le "start" et pas de champ pour le "end". Et si l'annotation est de type "range", il faut utiliser un champ pour le "start" et un champ pour le "end".
- mettre en page le lecteur avec la timeline dans un même conteneur. Il doivent avoir tout les deux la même largeur. Et la timeline doit être positionnée en bas du lecteur sans marge entre les 2.
- Comme les anotatins peuvent contenir deu text, sous la timeline, on peu faire apparaitre un bloc avec le text de l'annotation. Si plusieurs annotations se chevauchent, on peu faire apparaitre un bloc avec le text  pour chaque anotation. L'annotation qui apparait, doit être placé au dessus des autres. L'annotation doit apparaitre au moment ou elle est positionnée sur la timeline en fonction de la où en est le pointer de lecture du méda. Donc cela doit fonctionner même quand on déplace seulement le pointeur de lecture en mode pause ou arrêt ou quand un clic simple est réalisé sur la timeline. Une anotation doit apparaitre tant que le curseur de lecture n'a pas atteind sa fin et au minimum pendant 15s.